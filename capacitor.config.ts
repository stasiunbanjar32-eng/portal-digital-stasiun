import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.androidpro',
  appName: 'Portal Digital Stasiun Banjar',
  webDir: 'dist'
};

export default config;
